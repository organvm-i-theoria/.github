name: CodeQL

on:
  push:
    branches: [main]
    paths:
    - '**.py'
    - '**.js'
    - '**.ts'
    - '**.rb'
    - '**.go'
    - .github/workflows/codeql-analysis.yml
  pull_request:
    branches: [main]
    paths:
    - '**.py'
    - '**.js'
    - '**.ts'
    - '**.rb'
    - '**.go'
    - .github/workflows/codeql-analysis.yml
  schedule:
  - cron: 0 6 * * 1   # Weekly on Mondays
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [python, javascript, go, ruby]

    steps:
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # ratchet:actions/checkout@v4.2.2

    - name: Initialize CodeQL
      uses: github/codeql-action/init@9e8d0789d4a0063d491a732433ef9511853b6eb9 # v3.24.4
      with:
        languages: ${{ matrix.language }}

    - name: Autobuild
      uses: github/codeql-action/autobuild@9e8d0789d4a0063d491a732433ef9511853b6eb9 # v3.24.4

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@9e8d0789d4a0063d491a732433ef9511853b6eb9 # v3.24.4
      with:
        category: /language:${{matrix.language}}
